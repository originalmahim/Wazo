import { useLoaderData } from "react-router-dom";


const BrandShop = () => {
          const product = useLoaderData()
          console.log(product);
          return (
          <div>
                              
          </div>
          );
};

export default BrandShop;